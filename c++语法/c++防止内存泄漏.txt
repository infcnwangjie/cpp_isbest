stl容器无论是添加还是取出 都是拷贝，如果不想拷贝可以传指针；

传递了指针，并且在不使用容器的时候，需要循环遍历容器，删除指针指向的对象，再删除容器；

普通类使用指针也要做到防止内存泄漏，类A 引用指针P，需要在P中加入引用计数变量或者在类A中加入引用计数变量，推荐在P中添加引用技术，
这是面向对象编程的风格问题；在类A的析构函数中，if(--p->use==0 ){delete p;}

这两类是最典型的防止内存泄漏的办法。后续会持续维护